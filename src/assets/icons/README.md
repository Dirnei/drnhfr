Octicons by GitHub (MIT), vendored from primer/octicons:
github.svg = mark-github-16, project-page.svg = browser-16, external.svg = link-external-16.
`fill="currentColor"` was added to the root element; nothing else is changed.
