Vendored icons, `fill="currentColor"` added to the root element, nothing else changed.

Octicons by GitHub (MIT), from primer/octicons:
  github.svg = mark-github-16, project-page.svg = browser-16,
  external.svg = link-external-16, mail.svg = mail-16.

Simple Icons (CC0), from simple-icons:
  linkedin.svg (the <title> element was removed; the link text names it).
